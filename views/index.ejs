<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Home Page</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            padding: 20px;
        }

        form {
            margin-bottom: 20px;
        }

        input[type="text"] {
            padding: 8px;
            width: 300px;
            font-size: 16px;
        }

        button {
            padding: 8px 16px;
            font-size: 16px;
            cursor: pointer;
        }
        .banner img {
            padding-top:3px;
            width: 1382px;
            height: auto;
        }
        .searchbar {
            padding-left: 510px;
            margin-top: 50px;
            margin-bottom: 50px;
        }
        .container {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
        }

        .card {
            width: 30%;
            background-color: #fff;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }

        .card img {
            width: 100%;
            height: 200px;
            object-fit: cover;
        }

        .card-content {
            padding: 15px;
        }

        h2 {
            margin-top: 0;
            color: #4285f4;
        }

        p {
            color: #555;
        }

        .read-more {
            display: inline-block;
            margin-top: 10px;
            color: #4285f4;
            text-decoration: none;
        }
    </style>
</head>
<body>
    <%- include('partials/navbar') %>
    <div class="banner">
        <img src="/images/homepagebanner.jpg" alt="Banner Image">
    </div>
    <h2 style='position: relative; text-align: center; top:20px;'>Welcome to our website!</h2>
    <form class='searchbar' action="/search" method="GET">
        <input type="text" name="query" placeholder="Search books...">
        <button type="submit">🔍</button>
    </form>

    <div class="container">
        <% books.forEach(book => { %>
            <div class="card">
                <img src="images/<%=book.image %>" alt="<%= book.title %>">
                <div class="card-content">
                    <h2><%= book.title %></h2>
                    <p><%= book.author %></p>
                    <a href="/book-details/<%= book.bookId %>" class="read-more">Read more</a>
                </div>
            </div>
        <% }); %>
    </div>
    <br>
</body>
<%- include('partials/footer') %>
</html>
